#include "simulated_transmit_backend.hpp"
#include <cstdlib>
#include <iostream>
#include <stdexcept>
class C:public wsprrypi::IExecutionContext{public:bool stop{};std::vector<size_t> seen;bool stopRequested()const noexcept override{return stop;}bool waitInterruptibleFor(std::chrono::nanoseconds)override{return!stop;}void reportExecutionProgress(size_t i)noexcept override{seen.push_back(i);}std::chrono::nanoseconds logicalNow()const noexcept override{return{};}};void x(bool v,const char*m){if(!v)throw std::runtime_error(m);}wsprrypi::ExecutionPlan p(){wsprrypi::ExecutionPlan q;q.id.value=7;q.backend=wsprrypi::BackendKind::SIMULATED;q.mode=wsprrypi::TransmissionMode::WSPR;for(int i=0;i<4;i++)q.events.push_back({std::chrono::seconds{i},std::chrono::seconds{1},wsprrypi::RfEventType::SET_FREQUENCY,14097100.0+i,true});q.summary.total_duration=std::chrono::seconds{4};return q;}
int main()try{C c;auto q=p();wsprrypi::SimulatedTransmitBackend b(c);x(b.configure(q,{}).ok,"configure");x(b.execute(q).ok,"execute");auto first=b.traceJson();x(first.find("\"mode\":\"WSPR\"")!=std::string::npos,"mode");x(first.find("3000000000")!=std::string::npos,"time");x(b.cleanup().ok,"cleanup");auto once=b.traceJson();x(b.cleanup().ok&&b.traceJson()==once,"idempotent cleanup");x(b.configure(q,{}).ok&&b.execute(q).ok&&b.traceJson()==first,"repeat");wsprrypi::SimulatedBackendConfig f;f.fail_event=1;f.fail_cleanup=true;wsprrypi::SimulatedTransmitBackend bad(c,f);x(bad.configure(q,{}).ok&&bad.execute(q).faulted&&!bad.cleanup().ok,"faults");wsprrypi::SimulatedBackendConfig k;k.cancel_event=1;wsprrypi::SimulatedTransmitBackend cancelled(c,k);x(cancelled.configure(q,{}).ok&&cancelled.execute(q).stopped,"injected cancel");c.stop=true;wsprrypi::SimulatedTransmitBackend z(c);x(z.configure(q,{}).ok&&z.execute(q).stopped,"cancel");std::cout<<"simulated backend tests passed\n";return 0;}catch(const std::exception&e){std::cerr<<e.what()<<'\n';return 1;}
